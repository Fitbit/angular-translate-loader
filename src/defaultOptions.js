/**
 * @private
 */
const DEFAULT_OPTIONS = {
    module: 'translations',
    namespaces: '',
    sep: '/',
    localeInterpolate: /[a-z]{2}_[A-Z]{2}/,
    defaultLocale: 'en_US'
};

export default DEFAULT_OPTIONS;
